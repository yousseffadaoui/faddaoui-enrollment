# Pour exécution LOCALE avec Postgres dans Docker (docker-compose.db-only.yml)
# Lancer : java -jar ... --spring.profiles.active=docker,docker-local
# Credentials in URL to avoid encoding issues on some systems
spring.datasource.url=jdbc:postgresql://localhost:5432/courses_db?user=esm&password=esm_secret
