export const environment = {
  production: false,
  // Use relative paths so Angular dev proxy forwards to backend (avoids CORS / connection issues)
  apiUrl: '',
  enrollmentApiUrl: '/enrollment-api'
};
